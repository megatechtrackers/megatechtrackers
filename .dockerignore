# Dependencies and build artifacts (keep context small)
node_modules/
.next/
.turbo/
.nuxt/
.output/
coverage/
.cache/
*.tsbuildinfo
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Only root-level lib/lib64 (Python venv), not e.g. ops_node/frontend/lib
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Secrets (never send to build context)
.env
.env.local
.env.*.local

# Logs and DB files
logs/
*.db
*.sqlite

# IDE and OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Git (not needed in image)
.git/
.gitignore

# App dirs not used by any root-context Dockerfile (consumer/alarm/parser/camera/sms-gateway/monitoring/mock-*)
# They use their own build context or are standalone; excluding shrinks context for root builds.
access_control_node/
fleet-monitor/
frappe_apps/
mobile_app_node/
ops_node/
web_app_node/

# Docs (exclude most; allow specific ones if a Dockerfile needs them)
*.md
!README.md

