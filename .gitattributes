# Git Line Ending Configuration
# Ensures shell scripts always use Unix (LF) line endings

# Auto detect text files and normalize line endings to LF on checkin
* text=auto eol=lf

# Shell scripts must always use LF (Unix) line endings
*.sh text eol=lf

# Windows scripts should use CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
